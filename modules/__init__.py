"""Feature modules. Each may import from `core`; none may import from `ui`.

A module is a plain function library with no Streamlit calls, so it can be
tested on its own and prototyped in Colab before being wired into the app.
"""
