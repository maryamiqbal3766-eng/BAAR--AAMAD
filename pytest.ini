[pytest]
# Lets tests import `core` without installing the project as a package.
pythonpath = .
testpaths = tests

markers =
    live: hits the real Groq API. Skipped automatically unless GROQ_API_KEY is
        set, so the default suite never depends on a network or a key.
